In-depth instructions are currently only available in Icelandic.

Documentation is written using markdown and is compiled into PDFs with the `compile-docs` command that also resides in this directory.

Usage:

    ./compile-docs
